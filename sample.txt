navigateToMenubar();

waits.waitForInVisibility(loadingIcon);

elickobj.click(addResponsiblePartyButton, "Button to add Members");

List<WebElement> elements = driver.findElements(addResponsibleHtml);

WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(10));

for (WebElement ele : elements) {

    if (isInteractable(ele)) {

        try {

            WebElement firstNameInput = wait.until(
                    ExpectedConditions.elementToBeClickable(
                            ele.findElement(By.xpath(".//input[@placeholder='Enter first name']"))
                    )
            );

            firstNameInput.click();
            firstNameInput.sendKeys("hjjhskdajzd");

            ReportLogger.PasslogWithScreenshot("");

        } catch (Exception ex) {
            ReportLogger.log("Input still not interactable: " + ex.getMessage());
        }

    } else {
        ReportLogger.log("Not on top");
    }
}
